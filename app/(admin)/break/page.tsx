"use client";

const Break = () => {
  // const session = await auth();

  return (
    <div className="w-full h-full flex justify-center items-center">
      <p>Break screen will develop soon..</p>
    </div>
  );
};

export default Break;
