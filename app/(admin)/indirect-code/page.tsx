"use client";

const IndirectCode = () => {
  // const session = await auth();

  return (
    <div className="w-full h-full flex justify-center items-center">
      <p>IndirectCode screen will develop soon..</p>
    </div>
  );
};

export default IndirectCode;
