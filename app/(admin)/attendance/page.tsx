"use client";

const Attendance = () => {
  // const session = await auth();

  return (
    <div className="w-full h-full flex justify-center items-center">
      <p>Attendance screen will develop soon..</p>
    </div>
  );
};

export default Attendance;
