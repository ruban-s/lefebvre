"use client";

const Dashboard = () => {
  // const session = await auth();

  return (
    <div className="w-full h-full flex justify-center items-center">
      <p>Dashboard screen will develop soon..</p>
    </div>
  );
};

export default Dashboard;
