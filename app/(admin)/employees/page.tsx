"use client";

const Measures = () => {
  // const session = await auth();

  return (
    <div className="w-full h-full flex justify-center items-center">
      <p>Measures screen will develop soon..</p>
    </div>
  );
};

export default Measures;
