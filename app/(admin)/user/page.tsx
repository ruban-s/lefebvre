"use client";

const User = () => {
  // const session = await auth();

  return (
    <div className="w-full h-full flex justify-center items-center">
      <p>User screen will develop soon..</p>
    </div>
  );
};

export default User;
